# 项目状态快照（保持短小：建议 <= 200~400 行）

## 当前结论（必须最新）
- 现状：已新增 Markdown 引用式链接展开模块 `modules/markdown_reference_link_inliner.ahk`，并在 `modules/hotkeys.ahk` 挂载 `Ctrl+Alt+M` 热键，用于直接处理当前剪贴板里的 AI Markdown 文本。
- 已完成：实现引用定义解析与正文改写，支持 `[文本][标签]`、`[文本][]`、`[标签]` 以及图片引用 `![alt][img]`，成功改写时会自动移除文末定义行。
- 已验证：`tests/markdown_reference_link_inliner_tests.ahk` 已通过自动化测试，覆盖标准引用、折叠引用、快捷引用、大小写归一、标题内裸双引号、图片引用、未命中定义、非法定义、重复引用等 10 类场景；另完成 1 次真实样例冒烟验证。
- 下一步：让你实际 Reload 脚本后，在常用 AI/笔记工具里回归 `Ctrl+Alt+M`，重点确认“先复制正文再展开”“包含中文标题与查询参数链接”“正文中多次复用同一引用标签”这三类真实使用路径。

## 关键决策与理由（防止“吃书”）
- 决策A：Markdown 链接展开逻辑新建独立模块，而不是继续堆进 `modules/chat_message_cleaner.ahk` 或 `modules/hotkeys.ahk`。
  原因：这次处理对象虽然也是“剪贴板文本”，但语义已经从“聊天清洗”变成“Markdown 语法转换”；独立模块更便于单测和后续扩展。
- 决策B：只有在“检测到定义且正文确实发生替换”时，才移除文末定义区并写回剪贴板。
  原因：如果只有定义、没有正文引用，或者引用未命中定义，贸然删定义会破坏原文，稳定性比“强行改写”更重要。
- 决策C：生成行内链接时统一把 URL 包成 `<url>`，并对标题里的 `\` 与 `"` 做转义。
  原因：AI 给出的 URL 可能带括号、查询参数甚至标题内裸双引号；统一规范化后，迁移到别处时更不容易坏链。

## 常见坑 / 复现方法
- 坑1：`Ctrl+Alt+M` 只处理“当前剪贴板已有文本”，不会自动帮你先执行复制；若触发后提示未检测到定义，先确认你复制进去的是完整 Markdown 原文。
- 坑2：AI 生成的引用标题里可能直接夹裸双引号，例如 `"第十轮 "十四五" 规划教材"`；本次实现会尽量宽松解析并在输出时转义，但如果原始定义首尾包裹符本身不完整，仍会被当成非法定义忽略。
- 坑3：快捷引用 `[标签]` 只会在后面跟空白、结尾或常见标点时展开；这是故意的保守策略，用来避免把已有的行内链接 `[文本](url)` 或别的中括号语法误改坏。
